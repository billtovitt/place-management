import React from 'react';
import { shallow } from "enzyme";
import index from '../../places';

describe('testing places screen', () => {
  it('renders places screen', () => {
    <index />
  });
});
