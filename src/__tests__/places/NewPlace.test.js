import React from 'react';
import { shallow } from "enzyme";
import NewPlaceScreen from '../../places/NewPlace';

describe('testing new place screen', () => {
  it('renders new place screen', () => {
    <NewPlaceScreen />
  });
});
