import React from 'react';
import { shallow } from "enzyme";
import NewUserScreen from '../../users/NewUser';

describe('testing new user screen', () => {
  it('renders new user screen', () => {
    <NewUserScreen />
  });
});
